import {toggleMenu} from './toggle_menu.js';

// Lorsque la page est complètement chargée, lancer la fonction toggleMenu (menu burger)
document.addEventListener('DOMContentLoaded', () => {
    toggleMenu();    

})